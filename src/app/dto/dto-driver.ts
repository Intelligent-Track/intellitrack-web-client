export class DtoDriver {
    constructor(public id: number, public fullName: string, public email: string, public license: string, public servicesFinished: number, public incomingServices: number) { }
}

export class DtoDriver {
    constructor(public document: number, public name: string, public username: string, public job: string, public phone: number, public location: string, public directo: Boolean, private placaVehiculo: string, public licencia: string, public revisionAutoMec: string) { }
}

export class DtoMechanic {
    constructor(
        public fullName: string, public email: string, public id: number, public phoneNumber: number, public location: string, public managerName: string
    ){}
}

import { DtoWarehousePackage } from './dto-warehouse-package';

describe('DtoWarehousePackage', () => {
  it('should create an instance', () => {
    expect(new DtoWarehousePackage()).toBeTruthy();
  });
});

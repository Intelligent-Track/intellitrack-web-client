export class DtoOperator {
    constructor(
        public name: string, public username: string, public password: string, public document: number, public phone: number, public job: string, public location: string, public managerUsername: string
    ) { }
}

import { DtoLinkMechanicManager } from './dto-link-mechanic-manager';

describe('DtoLinkMechanicManager', () => {
  it('should create an instance', () => {
    expect(new DtoLinkMechanicManager()).toBeTruthy();
  });
});

import { DtoLinkOperatorManager } from './dto-link-operator-manager';

describe('DtoLinkOperatorManager', () => {
  it('should create an instance', () => {
    expect(new DtoLinkOperatorManager()).toBeTruthy();
  });
});

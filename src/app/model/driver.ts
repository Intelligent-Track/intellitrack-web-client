export class Driver {
    constructor(public id: number, public name: string, public username: string, public document: number, public phone: number, public job: string, public location: string, public external: Boolean, public managerUsername: string, public vehiclePlate: string, public license: FormData, public mecReview: FormData) { }
}
